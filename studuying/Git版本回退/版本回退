$ git log                      //查看当前所有的commit记录
$ git log --pretty=oneline     //查看当前所有的commit记录 带着commit id  这两个二选一

$ git reset --hard HEAD^       //回退到上一个版本
$ git reset --hard HEAD^^      //回退到上上个版本
$ git reset --hard HEAD~100    //回退到100个版本之前


$ git reset --hard 3628164     //随意回退到指定commit id的版本   hard后面的字符串可以不用写全  会自动查

$ git reflog                   //查看所有的git操作记录  可以看到所有的操作   用于想要版本回退或者版本恢复但是忘了commit id的时候